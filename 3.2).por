programa {
  funcao inicio() {
    real base, altura, area
    escreva("b(m)= ")
    leia (base)
    escreva("h(m) = ")
    leia (altura) 
    area = (base*altura)/2
    escreva("A área é de: "+area+" metros²")
  }
}
